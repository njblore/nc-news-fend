import React from 'react';

const VoteButtons = props => {
  return (
    <div className="vote-buttons">
      <button>⇧</button>
      <button>⇩</button>
    </div>
  );
};

export default VoteButtons;
